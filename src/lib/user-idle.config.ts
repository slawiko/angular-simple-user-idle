export class UserIdleConfig {
  /**
   * Timeout value (in seconds)
   */
  timeout?: number;

  /**
   * time (in milliseconds) how often activity should influence timer
   */
  sensitivityMilliseconds?: number;
}
